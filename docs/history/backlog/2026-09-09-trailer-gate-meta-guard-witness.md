# Backlog: trailer-gate arms-table meta-guards: negative witness + message disambiguation

Status: open
Date: 2026-09-09
Class: deferred review findings, regenerating-classes rule per ADR-0002 (duplicate-unit-witness / fix-generated polish family)
Source: `docs/reviews/2026-09-09-2026-09-09-plan-readiness-trailer-gate-r5-deferrals-code-review-r3.md` (round 3 targeted panel, zero blocking; both findings Low, non-blocking, valid but deferred to stop fix-regeneration)
Plan of record: `docs/plans/completed/2026-09-09-plan-readiness-trailer-gate-r5-deferrals.md`

## Context

The r5-deferrals execution landed unconditional, whitespace-tight arms-table guards in `scripts/plan_readiness.py` (`_selftest_decision_marker`): polarity check `(needle is None) != expect_ok` and needle-parts check `all(p.strip() for p in needle.split("|"))`, both explicit `raise AssertionError` (survive `python3 -O`). The r3 panel found two follow-ups that exist only because of this fix; folding further was judged regenerating (r1 F2 -> r2 F1 -> r3 F1/F2 on the same guard family), so both were deferred here instead of a fourth round.

## Finding 1: guard failure messages do not say which guard fired (correctness-completeness)

Both guards raise `AssertionError(suffix)` with only the arm suffix; a firing traceback does not distinguish polarity mismatch from an empty/whitespace-only needle part. Suggested fix: distinct messages, e.g. `f"needle/expect_ok polarity mismatch: {suffix}"` and `f"empty needle part: {suffix}"`.

Acceptance: firing either guard in a scratch run prints which guard fired alongside the arm suffix; all current rows still pass; selftest `ALL PASS`.

## Finding 2: meta-guards have no negative witness (testing)

Every current row satisfies both guards, so neither `raise` line is reachable in any selftest scenario; a regression weakening the guards flips zero selftest lines. Suggested fix: factor the per-row guard block into a `_validate_arm(suffix, needle, expect_ok)` helper and drive it once with two malformed rows (mis-paired polarity; needle with a whitespace-only `|` part), asserting each raises; or minimally run the selftest once under `python3 -O` as a documented validation step.

Acceptance: the meta-arm(s) go red if either guard is weakened, removed, or converted back to bare `assert`; green under the current code.

## Disposition constraints

- Land in the next plan touching the `_selftest_decision_marker` arms-table family (same owner window as `docs/history/backlog/2026-09-09-trailer-gate-tilde-side-arm-coverage.md`).
- Fix-risk rationale (recorded per receiving-review): both findings are Low, theoretical/plausible-edge, and generated by the immediately preceding fix; the non-interactive run records this rationale and defers instead of asking.
